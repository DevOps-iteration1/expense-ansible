format_version: 10
pipelines:
  mysql:
    group: defaultGroup
    materials:
      git-b844969:
        git: https://github.com/DevOps-iteration1/expense-ansible.git
        shallow_clone: false
        auto_update: true
        branch: main
    secure_variables:
      vault_token: AES:p5w3xpd1h78xAyNggBiGkg==:ZFKICRRrr5OA14zf1x9UjpVR/OVDsi/jaZGF9+qgDaH1ca1tJdgW+mhvaBsc0SAf
    stages:
      - DEV:
          fetch_materials: true
          environment_variables:
            env : dev
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
                - exec:
                    arguments:
                      - ansible.sh
                      - mysql
                    command: bash #bash ansible.sh
                    run_if: passed
      - QA :
          fetch_materials: true
          environment_variables:
            env: qa
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
                - exec:
                    arguments:
                      - ansible.sh
                      - mysql
                    command: bash #bash ansible.sh
                    run_if: passed
      - STAGE:
          fetch_materials: true
          environment_variables:
            env: stage
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
                - exec:
                    arguments:
                      - ansible.sh
                      - mysql
                    command: bash #bash ansible.sh
                    run_if: passed
      - PROD:
          fetch_materials: true
          environment_variables:
            env: prod
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
                - exec:
                    arguments:
                      - ansible.sh
                      - mysql
                    command: bash #bash ansible.sh
                    run_if: passed